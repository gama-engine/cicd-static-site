<<<<<<< HEAD
version: 0.2

phases:
  build:
    commands:
      - echo "Build started"
      - ls

artifacts:
  files:
    - '**/*'
=======
version: 0.2

phases:
  build:
    commands:
      - echo "Build started"
      - echo "No build required"

  post_build:
    commands:
      - echo "Invalidating CloudFront cache..."
      - aws cloudfront create-invalidation \
        --distribution-id $DISTRIBUTION_ID \
        --paths "/*"

artifacts:
  files:
    - "**/*"
>>>>>>> 7667c9f (second commit)
